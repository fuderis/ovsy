[package]
name = "power-agent"
edition = "2024"

[lib]
name = "root"

[dependencies]
agent = { path = "../../agent" }
tokio = { version = "1.50.0", features = ["full"] }
serde = { version = "1.0.228", features = ["derive"] }
