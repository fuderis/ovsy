[package]
name = "music-agent"
edition = "2024"

[lib]
name = "root"

[dependencies]
agent = { path = "../../agent" }
tokio = { version = "1.50.0", features = ["full"] }
serde = { version = "1.0.228", features = ["derive"] }
fuzzy-cmp = "0.1.3"
regex = "1.12.3"
